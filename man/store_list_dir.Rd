% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{store_list_dir}
\alias{store_list_dir}
\title{Retrieve keys and sub-prefixes under a given prefix (single depth)}
\usage{
store_list_dir(store, ...)
}
\arguments{
\item{store}{A \code{ZarrStore} object.}

\item{prefix}{A string prefix ending with \code{/}.}
}
\value{
A named list with two character vectors:
\itemize{
\item \code{keys}: keys that start with \code{prefix} and contain no further \code{/}.
\item \code{prefixes}: sub-prefixes (each ending with \code{/}) that appear under
\code{prefix}.
}

For example, if the store contains \code{"a/b"}, \code{"a/c"}, \code{"a/d/e"},
\code{"a/f/g"}, then \code{store_list_dir(store, "a/")} returns
\code{list(keys = c("a/b", "a/c"), prefixes = c("a/d/", "a/f/"))}.
}
\description{
Returns only the keys and prefixes directly under \code{prefix}, i.e. those that
do not contain an additional \code{/} after \code{prefix}.
}
