% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store_s3.R
\name{S3Store}
\alias{S3Store}
\title{S3 store}
\usage{
S3Store(bucket, prefix = "", ...)
}
\arguments{
\item{bucket}{Name of the S3 bucket.}

\item{prefix}{Optional key prefix (e.g. \code{"my-array/"}) applied to all
keys stored in this store. Must end with \code{"/"} if non-empty.}

\item{...}{Additional arguments passed to \code{\link[paws.storage:s3]{paws.storage::s3()}}, such as
\code{region}, \code{endpoint}, or \code{credentials}.}
}
\value{
An \code{S3Store} object.
}
\description{
A Zarr V3 store backed by an AWS S3 bucket. Keys map to S3 object keys
under an optional \code{prefix}. The store uses \pkg{paws.storage} for all S3
operations; credentials and region are resolved via the standard AWS
credential chain (environment variables, \verb{~/.aws/credentials}, instance
metadata, etc.).
}
